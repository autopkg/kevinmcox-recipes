Description: >-
  Downloads the latest version of the swiftpkg CLI package (https://github.com/codecarton/swiftpkg).
Identifier: com.github.kevinmcox.download.SwiftPkg
MinimumVersion: "2.9.0"

Input:
  NAME: SwiftPkg
  INCLUDE_PRERELEASES: ""

Process:
  - Processor: GitHubReleasesInfoProvider
    Arguments:
      github_repo: codecarton/swiftpkg
      asset_regex: ^swiftpkg-.*-cli\.pkg$
      include_prereleases: "%INCLUDE_PRERELEASES%"

  - Processor: URLDownloader
    Arguments:
      filename: "%NAME%-%version%.pkg"

  - Processor: EndOfCheckPhase

  - Processor: CodeSignatureVerifier
    Arguments:
      input_path: "%pathname%"
      expected_authority_names:
        - "Developer ID Installer: Code Carton, LLC (DPXY7JLK67)"
        - Developer ID Certification Authority
        - Apple Root CA
